# 🚀 Быстрый Деплой на VPS

## Подготовка VPS

### 1. Подключитесь к серверу
```bash
ssh root@YOUR_VPS_IP
```

### 2. Создайте директорию проекта
```bash
mkdir -p /opt/vless-reality
cd /opt/vless-reality
```

### 3. Загрузите файлы на сервер

**С вашего компьютера (Windows PowerShell):**
```powershell
# Загрузить все файлы
scp server.py root@YOUR_VPS_IP:/opt/vless-reality/
scp start_server.sh root@YOUR_VPS_IP:/opt/vless-reality/

# Или zip и распаковать
```

**Или клонируйте репозиторий на сервере:**
```bash
cd /opt/vless-reality
# скопируйте server.py сюда
```

### 4. Установите зависимости (если нужно)
```bash
apt update
apt install -y python3 python3-pip
```

## Запуск Сервера

### Вариант 1: Прямой запуск (для тестирования)
```bash
cd /opt/vless-reality
python3 server.py 443
```

**⚠️ ВАЖНО:** Сервер выведет UUID - **скопируйте его сразу!**

```
============================================================
[!] COPY THIS UUID TO YOUR CLIENT:

    a1b2c3d4-5e6f-7890-abcd-ef1234567890
============================================================
```

### Вариант 2: Через screen (чтобы не отключался)
```bash
# Установить screen
apt install -y screen

# Создать сессию
screen -S vless-server

# Запустить сервер
python3 server.py 443

# Отключиться: Ctrl+A затем D
# Вернуться: screen -r vless-server
```

### Вариант 3: Systemd сервис (production)
```bash
# Используйте deploy.sh из проекта
chmod +x deploy.sh
./deploy.sh
```

## Открытие Порта в Файрволе

### Ubuntu/Debian (ufw)
```bash
ufw allow 443/tcp
ufw reload
ufw status
```

### CentOS/RHEL (firewalld)
```bash
firewall-cmd --permanent --add-port=443/tcp
firewall-cmd --reload
firewall-cmd --list-ports
```

### Проверка порта
```bash
netstat -tlnp | grep 443
```

## Подключение с Компьютера

### 1. Консольный клиент

**Windows:**
```powershell
python client.py a1b2c3d4-5e6f-7890-abcd-ef1234567890 YOUR_VPS_IP 443
```

**Linux:**
```bash
python3 client.py a1b2c3d4-5e6f-7890-abcd-ef1234567890 YOUR_VPS_IP 443
```

### 2. GUI клиент (рекомендуется)

**Windows:**
```batch
python client_gui.py
```

В интерфейсе:
1. UUID: `a1b2c3d4-5e6f-7890-abcd-ef1234567890` (вставьте скопированный)
2. Сервер: `YOUR_VPS_IP:443`
3. Нажмите "Подключиться"

## Проверка Соединения

### После подключения консольного клиента:
```
[Message] > test
```
Или отправьте любое сообщение для проверки.

### В GUI клиенте:
Просто напишите сообщение в поле и нажмите "Отправить".

### Тестирование с другого компьютера
```bash
# Проверка порта
telnet YOUR_VPS_IP 443

# Тест decoy mechanism
curl http://YOUR_VPS_IP:443
# Должно вернуть: HTTP/1.1 404 Not Found
```

## Мониторинг Сервера

### Просмотр логов (если используется screen)
```bash
screen -r vless-server
# Ctrl+A затем D для выхода
```

### Просмотр логов (если используется systemd)
```bash
journalctl -u vless-reality -f
```

### Проверка статуса
```bash
# Процессы
ps aux | grep server.py

# Соединения
netstat -an | grep :443

# Ресурсы
htop
```

## Остановка Сервера

### Screen
```bash
screen -r vless-server
# Ctrl+C
```

### Systemd
```bash
systemctl stop vless-reality
```

### Прямой запуск
```bash
# Ctrl+C в терминале
# или
pkill -f server.py
```

## Troubleshooting

### Не могу подключиться
1. Проверьте, что сервер запущен: `ps aux | grep server.py`
2. Проверьте порт: `netstat -tlnp | grep 443`
3. Проверьте файрволл: `ufw status` или `firewall-cmd --list-ports`
4. Проверьте UUID - он должен совпадать!

### Порт 443 занят
```bash
# Узнать, что использует порт
lsof -i :443

# Остановить конфликтующий сервис (например, apache)
systemctl stop apache2
```

### Permission denied на порту 443
```bash
# Запускайте с sudo или root
sudo python3 server.py 443
```

### Connection refused
- Проверьте IP адрес VPS
- Проверьте, что сервер слушает на 0.0.0.0, а не 127.0.0.1
- Проверьте облачный файрволл (AWS Security Groups, etc.)

## Security Checklist

Перед production использованием:

- [ ] UUID сгенерирован автоматически (не дефолтный)
- [ ] Порт 443 открыт только для нужных IP
- [ ] Настроен IP whitelist в `server.py` (если нужно)
- [ ] Используется systemd для автозапуска
- [ ] Настроен Fail2Ban
- [ ] Логи мониторятся

## Быстрая Команда для Полного Деплоя

```bash
# На VPS (одной командой)
cd /opt/vless-reality && \
chmod +x deploy.sh && \
./deploy.sh

# Скопируйте UUID из вывода
# Подключайтесь с клиента!
```

---

**Готово!** Теперь у вас работающий VLESS-Reality сервер на VPS.
