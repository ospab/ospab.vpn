# Сценарий Демонстрации для Жюри

**Олимпиада по ИБ - Муниципальный Этап**

## 🎬 План Презентации (5-7 минут)

### 1. Введение (30 секунд)
```
"Представляю реализацию протокола VLESS-Reality - технологию обхода 
цензуры нового поколения. В отличие от классических VPN, которые легко 
детектируются DPI системами, Reality маскируется под обычный HTTPS трафик 
к популярным сайтам."
```

### 2. Демонстрация Decoy Механизма (1 минута)

**Терминал 1**: Запустить сервер
```powershell
.\start_server.ps1
```

**Показать вывод**:
- UUID для аутентификации
- Reality SNI (www.microsoft.com)
- Listening на порту 4433

**Терминал 2**: Имитация неавторизованного подключения
```powershell
curl http://127.0.0.1:4433
```

**Объяснить**:
> "Видите? Сервер отвечает как обычный веб-сайт - HTTP 404. 
> Для DPI-системы это выглядит как неудачная попытка подключения 
> к Microsoft. Никаких признаков VPN."

### 3. Демонстрация VLESS Handshake (2 минуты)

**Терминал 3**: Запустить клиент
```powershell
.\start_client.ps1
```

**Показать в логах клиента**:
1. Генерация Reality payload с magic header
2. Отправка данных чанками по 50 байт
3. Успешное получение ответа от сервера

**Показать в логах сервера**:
1. Обнаружение magic header
2. Успешная аутентификация
3. Запуск туннеля

**Объяснить технику**:
> "Magic header (VLES) встроен в рандомную позицию TLS ClientHello.
> Для DPI это легитимный TLS handshake, но сервер его распознает."

### 4. Интерактивная Демонстрация (2 минуты)

**В клиенте**: Отправить несколько сообщений
```
Enter message: Проверка обхода цензуры
Enter message: Передача конфиденциальных данных
Enter message: exit
```

**Показать**:
- Сообщения проходят через туннель
- Сервер эхо-отвечает с префиксом
- Соединение остается активным

**Объяснить защиту**:
> "Обратите внимание на traffic splitting - данные идут маленькими 
> порциями с задержками. Это имитирует человеческий ввод в браузере 
> и ломает паттерны DPI-анализа."

### 5. Технические Детали (1.5 минуты)

**Показать код** (`client.py` - функция `generate_fingerprinted_reality_payload`):
```python
# Random insertion point
insertion_point = random.randint(10, len(client_hello_part) - 5)

reality_payload = (
    tls_header + 
    client_hello_part[:insertion_point] + 
    VLESS_MAGIC_HEADER + 
    client_hello_part[insertion_point:] +
    ...
)
```

**Объяснить**:
> "Ключ успеха - рандомизация. Каждое соединение выглядит по-разному,
> нет фиксированного паттерна для блокировки."

**Показать** (`client.py` - traffic splitting):
```python
for i in range(0, len(vless_data), CHUNK_SIZE):
    chunk = vless_data[i:i + CHUNK_SIZE]
    writer.write(chunk)
    await asyncio.sleep(random.uniform(0.01, 0.05))
```

**Объяснить**:
> "Чанки по 50 байт + случайные задержки = невозможно построить 
> сигнатуру для DPI фильтрации."

### 6. Вопросы и Ответы

#### Возможные вопросы жюри:

**Q: "Почему именно www.microsoft.com?"**
> A: "Это популярный легитимный домен с высоким трафиком. Блокировка 
> его SNI нарушит работу миллионов сервисов. Можно использовать 
> любой CDN - Cloudflare, Google, Amazon."

**Q: "Как защищаетесь от active probing?"**
> A: "Decoy механизм. При отсутствии magic header сервер ведет себя 
> как обычный сайт - отдает HTTP 404. GFW не может отличить его 
> от настоящего."

**Q: "Почему UUID общий, а не генерируется?"**
> A: "UUID - это pre-shared secret. В реальности его передают через 
> защищенный канал заранее. Здесь hardcoded для упрощения демо."

**Q: "Что с шифрованием?"**
> A: "В полной версии VLESS payload шифруется AES-128-GCM. Это mock 
> для демонстрации концепции обхода DPI, не production решение."

**Q: "Можно ли детектировать такой трафик?"**
> A: "Теоретически - да, через statistical analysis или ML модели.
> Но это требует огромных вычислительных ресурсов и дает много 
> ложных срабатываний. Экономически нецелесообразно."

## 📝 Чек-лист Перед Демо

- [ ] Оба PowerShell скрипта (`start_server.ps1`, `start_client.ps1`) работают
- [ ] Python 3.7+ установлен
- [ ] 3 терминала открыты и готовы
- [ ] `curl` установлен (или готов Postman/браузер)
- [ ] Файлы `client.py` и `server.py` открыты в редакторе для показа кода
- [ ] README.md готов для справки

## 💡 Советы по Презентации

1. **Начинайте с проблемы**: "VPN легко блокируются, нужна альтернатива"
2. **Покажите сначала decoy**: Это самый впечатляющий момент
3. **Объясняйте каждый лог**: Не просто запускайте - комментируйте
4. **Подчеркивайте рандомизацию**: Это ключ к обходу DPI
5. **Будьте честны про limitations**: Mock реализация, не production
6. **Знайте код наизусть**: Готовьтесь к детальным вопросам

## 🏆 Сильные Стороны Проекта

- ✅ Реальная работающая демонстрация
- ✅ Актуальная технология (Reality - 2023-2024)
- ✅ Понимание механизмов обхода цензуры
- ✅ Чистый асинхронный код на asyncio
- ✅ Документация и комментарии
- ✅ Ready-to-run скрипты

Удачи на олимпиаде! 🚀
